# Reference-only material, not used by the live site
# (newsroom/ itself also holds the images newsroom.html actually uses —
# only the saved source pages + their asset dumps are excluded, below)
newsroom/*.html
newsroom/*_files/
video/
Cloud.png*.png
Energy.html

# Source docx/txt handoff files for careers copy — content already
# transcribed into careers.html, not used by the live site.
Career/Opennings/
